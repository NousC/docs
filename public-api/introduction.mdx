---
title: "Public API"
description: "The v2 Context API — eight endpoints. The agent reads engineered, epistemics-tagged context and writes observations. Nous derives the rest."
---

The Nous Public API is a thin HTTP surface around the **evidence substrate**: every fact Nous knows comes from an observation, every claim carries its source, freshness, and confidence, and the agent never overwrites — it observes.

## Base URL

```
https://api.opennous.cloud
```

Self-hosting? Point at your own deployment.

## Authentication

Every request takes a workspace API key in the `Authorization` header. Create one at **Settings → API Keys** in your workspace.

```bash
curl https://api.opennous.cloud/v2/attention \
  -H 'Authorization: Bearer YOUR_API_KEY'
```

Keys are workspace-scoped — no separate workspace ID required.

## The eight endpoints

<CardGroup cols={2}>
  <Card title="POST /v2/context" href="/public-api/context" icon="brain">
    Engineered context for a task about one entity — the ranked, claim-tagged block your agent consumes before acting.
  </Card>
  <Card title="GET /v2/accounts/:id" href="/public-api/accounts" icon="user">
    The full Account Record — entity + every claim with epistemics + recent timeline.
  </Card>
  <Card title="POST /v2/observations" href="/public-api/observations" icon="pen">
    Record what happened. You observe; Nous derives the claims.
  </Card>
  <Card title="POST /v2/query" href="/public-api/query" icon="magnifying-glass">
    Retrieve + summarise a corpus of observations across many entities.
  </Card>
  <Card title="GET /v2/attention" href="/public-api/attention" icon="bell">
    Workspace-wide: accounts gone quiet, facts decayed — ranked decisions.
  </Card>
  <Card title="POST /v2/verify" href="/public-api/verify" icon="shield-check">
    Re-check one claim against current observations — the calibration check.
  </Card>
  <Card title="POST /v2/dedup" href="/public-api/dedup" icon="filter">
    Cross-list cold-outbound dedup — net_new / engaged / bounced / unsubscribed.
  </Card>
  <Card title="GET /v2/workspace/facts" href="/public-api/workspace-facts" icon="book">
    The user's own playbook — ICP, market, pricing, competitors.
  </Card>
</CardGroup>

## Identifiers are universal

Most endpoints accept a `focus` parameter. Pass any of:

- an **entity UUID** (`a1b2c3d4-...`)
- an **email** (`sarah@acme.com`)
- a **domain** (`acme.com`)
- a **LinkedIn URL** (`https://linkedin.com/in/sarah-chen`)
- a **name** (`Sarah Chen`)

A name may match several people — the response will be `{ status: "ambiguous", candidates: [...] }` so your agent can pick one and re-call.

## Epistemics on every claim

Every claim Nous returns carries three signals so your agent can decide whether to act on it:

| Field | Values | Meaning |
|---|---|---|
| `freshness` | `fresh` / `aging` / `suspect` / `expired` | How recently it was observed |
| `confidence` | `0.0` – `1.0` | How strongly the observations support it |
| `epistemic_class` | `observed` / `derived` / `asserted` | Where the claim came from |

Agents that ship rely on `freshness === 'fresh' && confidence > 0.7` before acting. Anything weaker — call `/v2/verify` first.

## SDKs

Prefer code over curl? Both wrap the same endpoints:

<CardGroup cols={2}>
  <Card title="TypeScript / Node" href="https://www.npmjs.com/package/@opennous/sdk" icon="js">
    `npm install @opennous/sdk`
  </Card>
  <Card title="Python" href="https://pypi.org/project/opennous" icon="python">
    `pip install opennous`
  </Card>
</CardGroup>

## Errors

All errors return JSON with an `error` code:

```json
{ "error": "entity_not_found" }
```

| Status | Error | What to do |
|---|---|---|
| 400 | `focus_required`, `invalid_focus`, etc. | Missing or malformed parameter |
| 401 | `unauthorized` | API key missing or invalid |
| 402 | `feature_not_in_plan` | Upgrade — visible on the Usage page |
| 404 | `entity_not_found` | Focus didn't match any entity |
| 413 | `too_many` | Reduce batch size |
| 429 | `rate_limited` | Honor `Retry-After` |
| 500 | `internal_error` | Retry with backoff |
